eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://127.0.0.1:8011/eureka,http://127.0.0.1:8012/eureka
server:
  port: 8010
spring:
  application:
    name: eureka-server